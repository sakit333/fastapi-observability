---
apiVersion: v1
kind: ConfigMap
metadata:
  name: tempo-config
data:
  tempo.yml: |
    server:
      http_listen_port: 3200

    distributor:
      receivers:
        otlp:
          protocols:
            grpc:
            http:

    # 🔥 REQUIRED FOR rate()
    metrics_generator:
      storage:
        path: /tmp/tempo/generator/wal
      processor:
        service_graphs: {}
        span_metrics: {}

    # 🔥 REQUIRED to activate processors
    overrides:
      metrics_generator_processors: [service-graphs, span-metrics]

    # 🔥 REQUIRED for query path
    querier: {}

    storage:
      trace:
        backend: local
        local:
          path: /tmp/tempo

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tempo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: tempo
  template:
    metadata:
      labels:
        app: tempo
    spec:
      containers:
        - name: tempo
          image: grafana/tempo:2.5.0
          imagePullPolicy: Always
          args:
            - "-config.file=/etc/tempo/tempo.yml"

          ports:
            - containerPort: 3200
            - containerPort: 4317
            - containerPort: 4318

          volumeMounts:
            - name: config
              mountPath: /etc/tempo
            - name: tempo-data
              mountPath: /tmp/tempo

          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

      volumes:
        - name: config
          configMap:
            name: tempo-config
        - name: tempo-data
          emptyDir: {}

---
apiVersion: v1
kind: Service
metadata:
  name: tempo
spec:
  selector:
    app: tempo
  ports:
    - name: http
      port: 3200
      targetPort: 3200
    - name: grpc
      port: 9095
      targetPort: 9095
    - name: otlp
      port: 4317
      targetPort: 4317
